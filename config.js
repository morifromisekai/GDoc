// Shared Blog Configuration
const CONFIG = {
    // Default Git settings (these can be updated via the blogger dashboard)
    GITHUB_OWNER: "morifromisekai",
    GITHUB_REPO: "GDoc",
    GITHUB_BRANCH: "master" // GitHub default branch (e.g., master or main)
};
